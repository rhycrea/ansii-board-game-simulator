public class Dwarfs extends Calliance implements InterfaceOfChars {

	public Dwarfs(int maxHP, int HP, int AP, int maxMove, char attackType, int x, int y, char side,
			char type, String icon) {
		super( maxHP, HP, AP, maxMove, attackType, x, y, side, type, icon );
	}

	public Dwarfs() {

	}
}
